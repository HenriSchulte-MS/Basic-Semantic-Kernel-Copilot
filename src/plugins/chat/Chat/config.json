{
    "schema": 1,
    "description": "General-purpose function for chatting with the user. Can call functions to solve more complex tasks.",
    "execution_settings": {
        "default": {
            "max_tokens": 4000,
            "temperature": 0.3
        }
    },
    "input_variables": [
        {
            "name": "request",
            "description": "The user's request.",
            "default": ""
        },
        {
            "name": "history",
            "description": "The history of the conversation.",
            "default": ""
        }
    ]
}